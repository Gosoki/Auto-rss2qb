# 待拍板清单（五轮审计合并版）

> 四份轮次报告里的 Q/D 项去重、去过时之后是 **24 条**。按「影响 ÷ 决策成本」排序——
> 排在前面的是「一句话就能定、定了立刻解锁一批工作」的。
>
> **这份文件是五轮循环最重要的交付物之一。** 之前每一轮的 agent 都要重新读一遍散在四份报告里的问题、
> 重新推导一遍利弊（插件方案的重复度数字被独立重算过三次）——那是整个循环里最大的一块重复劳动。
>
> 已实施的不在此列（见各轮报告）；**标 ✅ 的是我按推荐先实施了、你若不同意可以回退的**。

## 最优先

| # | 问题 | 选项 | 推荐 | 不决策的后果 |
|---|---|---|---|---|
| **D-28** | **`tests/` 要不要入库？** | A: 入库 · B: 保持不入库 | **A** | 430+ 个用例、7 秒、零网络、不碰开发库，每条对应一次真实修过的 bug。它现在**只存在于这一台机器的一个目录里**，而 `.gitignore` 还专门保证它不会被带走。重装 / 换机 / 误删 = 五轮审计的全部回归网一次归零。**这是清单里唯一一条「不决策本身就在持续损失」的** |
| **D-01** | MySQL 后端是否标为「实验性」？ | A: README 标一句，四条 MySQL 缺陷整组降 P3 · B: 当正式后端，四条排进下一批 | **A**（除非你真在跑 MySQL） | 四条 MySQL 缺陷（超长 alias 重复建番、半库迁移、DDL 中断不自愈、迁移期热源库）已连挂五轮，每轮都被重新评估一遍，纯消耗 |
| **D-05** | SSRF 守卫覆盖到哪？ | A: 首跳也强制内网判定 · B: 只对**重定向后的跳**强制，首跳（用户自填）放行 | **B** | 挂了四轮。被攻陷的源站一个 302 就能让常驻协程周期性对同机 qB 发可控 GET（审查员实测收到 `torrents/delete?hashes=all`）。A 会打断「自建 Mikan/RSS 镜像」这一合理用法 |
| **D-06** | bgm 首次识别要不要移出采集主循环？ | A: 只加时间预算（约 6 行、行为不变） · B: A + 首次识别移出 `poll_once` | **A 先做** | 挂了四轮。单番最坏 ~22 分钟，20 个新番能堵住整轮采集与放行数小时，而 nyaa 滑动窗口滚掉的条目**永不再采**。A 能把最坏值压到「每番 2 分钟」 |
| **D-03** | 「已完结·已停订」要不要成为列表徽标的第三态？ | A: 加第三态 + tooltip 指向『继续订阅』 · B: 只改两处文案 | **A** | 开了停订的用户现在看到的是「待确认」（而那个页面里没有它）和「这一集在锁定源下没有可下的」（一句错话），会去查两个他可能压根没设过的设置 |

## 架构与重构

| # | 问题 | 选项 | 推荐 | 不决策的后果 |
|---|---|---|---|---|
| **D-07** | 插件架构走 C+ 吗？ | A: 合并 `_parse` 为 `RssSource` + 显式 `SOURCES` 字典（分两步） · B: 只合并 `_parse` · C: 上注册表 · D: 都不动 | **A，分两步**，且先给 `_parse` 补直驱用例 | 源层的重复**已经开始产生缺陷而不只是成本**：第 5 轮的「字幕组白名单还是大小写敏感」正是第 9 次「同一改动改两遍」的产物 |
| **D-08** | flush / 标注 / 补下 三条挑选口径统一吗？ | A: `_PLAN_COLS` 补 `retry_count` **并且**让 flush 也用 `prefer_fresh` · B: 保持现状 | **A，但必须整体做** | 只做一半是明确的回归。标注侧与后台自动下载看到的「第一条候选」在失败场景下会分家 |
| **D-24** | 命名整理批次做不做？ | 7 条一次做完 或 一次不做 | **一次做完或一次不做** | 零散改名比不改更糟（grep 会出现两代名字）。命名误导已有实证：曾有人按名字以为 `is_subscribed_row` 是完整判据 |
| **D-17** ✅ | `enrich.resolve` 改成真三态吗？ | A: 改签名 · B: 计数器按对端拆开 | **已按 B 实施** | B 是把现状做对，A 才是消掉这类问题（「用一个全局计数器的差值表达三态」是三条缺陷的共同来源） |

## 产品行为

| # | 问题 | 选项 | 推荐 | 不决策的后果 |
|---|---|---|---|---|
| **D-02** ✅ | 关掉「判定完结」应该是什么语义？ | A: 只停「打标记+推送」，撤销无条件跑 · B: 消费侧一律 `ENABLED and UNSUB` · C: 关掉时清空全部标记 | **已按 A 实施** | 不修的话：用户为了止损去关开关，反而把误判**永久冻住**（消费侧看的是另一个开关） |
| **D-09** | `_merge_movie` 的「双方都已下过就拒绝合并」护栏 | A: 加 · B: 不加 | **A（已加）** | 一次误识别 = 静默删除一行 Movie 及其已下版本，UI 还弹绿色「识别成功 ✓」 |
| **D-11** | `DOWN_PATH` 默认 `/home` 改不改？ | A: 改成空串 + 首配提示 · B: 保持 | **A** | `/home` 是系统目录，且首配链路上没有任何一处会拦（预填值恒满足校验）——直接点保存就把番下进 `/home/26C/<番名>` |
| **D-14** | `apply_bgm_meta` 的 `keep_path`：改代码还是改注释？ | A: 改代码 · B: 改注释 + 单独快照三字段 | **B** | A 会把「目录名不好看」换成「永远修不好」 |
| **D-18** | 剧场版通知复用 `delivered` 还是单列一键？ | A: 单列 · B: 复用 | **A** | 用户关不掉剧场版推送而保留番剧推送。**注意 A 会触发 D-22 那条路径**，两条一起改 |
| **D-12** | 剧场版：`mikan_id` 唯一约束 + 「刷新版本」按钮 | A: 一起做 · B: 只做按钮 · C: 都不做 | **A** | `mikan_id` 的注释「刷新种子 RSS 用」从未兑现；BD 普遍首映后 6~18 个月才出，「每次人工整年重扫」实践中等于不会发生 |
| **D-25** | 番剧列表卡片化？ | A/B/C | **C 保持** | 无。番剧动辄几百部，卡片化必须同时上虚拟滚动（全站零虚拟滚动） |
| **D-26** | 剧场版侧「版本」「可下载」这两个词保不保？ | A: 全站统一 · B: 保留并写进术语表当**有意区分** | **B** | 无。一部剧场版确实有多个发布版本，与番剧的「一集一份」语义不同 |

## 细节

| # | 问题 | 推荐 | 备注 |
|---|---|---|---|
| **D-04** ✅ | 状态型通知的限流 | 已按「独立小桶」实施 | 被挡下时返回 False，配合「没发出去就不落状态记忆」，边沿下一轮自动补发 |
| **D-10** | `missingFiles` 补查脱离「搭车」？ | **B**：给页面『立刻刷新』开一条例外路径 | 人工点刷新正是该单独唤醒的时机。现状：休播期修好文件可能数周无人发现 |
| **D-13** | `/api/qb/done` 免 token 分支删不删？ | **B 已落地**，A 作为下一版默认 | A 会让已配好回调的用户失效一次 |
| **D-15** | `deploy.sh` 安全默认 | **C 保持**（已做），B（`User=autorss`）值得单独做并实测 | A 推错网段会把用户挡在自己面板外，无头 LXC 上不好救 |
| **D-16** | qB API Key（≥5.2） | **B 够用** | 负缓存已堵住封禁问题 |
| **D-19** | `_apply_bgm` 改 `total_episodes` 时同步清 `finished_at` | **A**（一行） | 只是让撤销更快；前提是 D-02 已定 |
| **D-20** | 断更 4 倍窗口的语义 | **A**：`idle_notified_at is None` 的番豁免出下界 | 保证每部番退出窗口前一定成功送达过一次。现在窗口是一条**暗规则**（已写进注释与设置页，但豁免未做） |
| **D-21** | `finished` 事件的跨重启去重锚点 | **A 先做**（`key=str(anime_id)` + 长 cooldown），B 是正解但要开 revision | 掉进/掉出 coverage 每翻一次就多一条重复推送 |
| **D-22** ✅ | 用户全部取消通知后，新版本的新事件要不要并进来？ | 已按 A（空集短路）实施 | 设置页写的是「留空＝一条都不发」 |
| **D-23** ✅ | 断更正文按「静默最久」还是「刚出事」排？ | 已按 A（刚出事优先）实施 | 此前注释与代码互相矛盾 |
| **D-27** | 新增 `NYAA_BASE` 配置键？ | **B 不加** | feed 字段对完整 URL 原样透传，走镜像的用户粘地址即可 |
