nicegui>=1.4
sqlmodel>=0.0.14
httpx>=0.27
feedparser>=6.0
python-dotenv>=1.0
opencc-python-reimplemented   # 繁转简；没装也能跑（自动降级为不转换）
# 可选：业务数据要落 MySQL 时才需要（设置页→数据库）。没装也能跑，只是切不了 MySQL、
# 面板上会提示装它；配置与默认的本地 SQLite 完全不依赖它。
pymysql>=1.1
# 数据库版本管理：建表/升级一律走 Alembic（db/schema.py + alembic/），两种后端共用一条版本链
alembic>=1.13

# 测试（tests/ 已入库，见 docs/DECISIONS.md D-28）。运行：.venv/bin/python -m pytest
# 【不是可选的】没有它，clone 下来的人跑不了那 470 多条回归——而那份回归网正是
# 这个项目唯一能挡住"改一处、坏另一处"的东西。零网络、约 10 秒。
pytest>=8.0
pytest-asyncio>=0.23
