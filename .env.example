# 复制为 .env。绝大多数设置在 Web「设置」/「源管理」页里改（存数据库、即时生效），
# 首次启动会把默认值写进数据库。这里只留改了要重启的结构项。.env 不要提交。

# Web 端口（改了需重启）。默认 2333——刻意【避开 8080】：那是 qB WebUI 的默认端口，
# 也是本项目 QB_URL 的默认值（config.py），同机跑 qB 时撞端口会让本服务起不来（Errno 98）。
WEB_PORT=2333

# 数据库文件路径（不填默认 data/autorss.db；改了需重启）
# DB_PATH=data/autorss.db
