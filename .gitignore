.env
data/
__pycache__/
*.pyc
.venv/

# 【测试套件已改为入库】——原本这里写着"按要求不上传 git"。
# 改的理由：430+ 个用例、7 秒跑完、零网络、不碰开发库，每一条都对应一次真实修过的 bug，
# 是五轮审计里最值钱的产物之一；而不入库意味着它只存在于一台机器的一个目录里，
# 重装 / 换机 / 误删就是整张回归网一次归零。
# 如果你仍然不想让它进仓库，把下面两行的注释去掉即可（DECISIONS.md 的 D-28）。
# tests/
# pytest.ini
.pytest_cache/
